<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:background="@drawable/grad"
        android:id="@+id/profile_grad"/>

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/profile_grad"
        android:layout_marginTop="-32dp"
        android:background="@drawable/border"
        android:id="@+id/add_leader_title">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginHorizontal="9dp"
            android:text="Add Project leader to project"
            android:layout_centerHorizontal="true"
            android:textSize="20sp"
            android:textStyle="bold"/>

    </RelativeLayout>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginHorizontal="30dp"
        android:layout_below="@id/add_leader_title">

        <AutoCompleteTextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="50dp"
            android:hint="@string/autocomplete_employee"
            android:id="@+id/add_leader_employee_selector"/>

        <AutoCompleteTextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="50dp"
            android:layout_below="@id/add_leader_employee_selector"
            android:hint="@string/autocomplete_project"
            android:id="@+id/add_leader_project_selector"/>

        <Button
            android:layout_width="220dp"
            android:layout_height="60dp"
            android:layout_marginTop="50dp"
            android:layout_centerHorizontal="true"
            android:layout_below="@id/add_leader_project_selector"
            android:text="Add New Project"
            android:textColor="#FFF"
            android:backgroundTint="@color/colorPrimary"
            android:id="@+id/add_leader_button"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/add_leader_button"
            android:text=""
            android:textAlignment="center"
            android:layout_marginTop="10dp"
            android:id="@+id/add_leader_status"/>

    </RelativeLayout>

</RelativeLayout>